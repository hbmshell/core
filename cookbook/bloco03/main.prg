

FUNCTION Main
LOCAL b1, b2

   b1 := {||
            ? "I CAN OCCUPY MANY LINES AND COMMANDS."
            ? ""
            ? ""
            ? "I DON'T NEED A ';' TO SEPARATE THE LINES"
            RETURN "Return EVAL function"
         }
   
   ? EVAL( b1 )         

          
RETURN NIL

