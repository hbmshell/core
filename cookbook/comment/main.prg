PROCEDURE Main

   ? "Hello World" // Comment in same line
   // ? "Comment at start of line."
   *  ? "Comment at start of line."
   && ? "Comment at start of line."

   ? "O valor : " ,  1200  // Util to explanation
                           

/*

    This comment are many lines

*/

RETURN